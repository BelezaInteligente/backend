const { resolve } = require('path');

module.exports = {
  config: resolve('config', 'database.config.js'),
  'migrations-path': resolve('database', 'migrations'),
  'models-path': resolve('app', 'models'),
  'seeders-path': resolve('database', 'seeders'),
};